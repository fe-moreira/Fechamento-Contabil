import Manual from './Manual'

// A tela de Ajuda é o Manual do Time — documentação viva, versionada no repo (atualiza
// junto com o sistema) e com botão "Gerar PDF".
export default function Ajuda() {
  return <Manual />
}
